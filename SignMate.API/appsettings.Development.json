{
  // Chỉ override những gì KHÁC appsettings.json cho môi trường Development.
  // Secret (DB / JWT / Gemini / VnPay / Email) nằm ở appsettings.Local.json (đã .gitignore).
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information"
    }
  }
}
